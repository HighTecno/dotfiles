# ============================================================
# ~/.zshenv — ALWAYS LOADED (keep minimal!)
# ============================================================


# Only put critical environment variables here
export ZDOTDIR="$HOME"


# Avoid heavy operations here — no plugins, no compinit, etc.
